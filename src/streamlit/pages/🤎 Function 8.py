import streamlit as st
import time

with st.empty():
    while(True):
        st.write("login page")
        if(True):
            st.write("home page")
            break
        else:
            st.write("21312321")
    st.write("home page 2")